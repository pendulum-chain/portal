import SwapPools from '../../../components/Pools/Swap';

const SwapPoolsPage = (): JSX.Element | null => {
  return (
    <div id="swap-pools" className="mt-8">
      <SwapPools />
    </div>
  );
};
export default SwapPoolsPage;
